# V2 backend (FastAPI). Cross-platform; no Windows-only deps in the core.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-docx>=1.1
pypdf>=5.0
httpx>=0.27
python-multipart>=0.0.9   # profile source uploads

# LLM providers (used only when AI tailoring is enabled)
anthropic>=0.40
openai>=1.40

# Windows-only: lets us render PDF via MS Word when LibreOffice isn't installed.
# On macOS/Linux, PDF is produced by LibreOffice headless (a documented prereq).
pywin32>=306; sys_platform == "win32"
