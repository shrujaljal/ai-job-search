# V2 backend (FastAPI). Cross-platform; no Windows-only deps in the core.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-docx>=1.1
httpx>=0.27
python-multipart>=0.0.9   # file uploads (custom résumé templates)

# LLM providers (used only when AI tailoring is enabled)
anthropic>=0.40
openai>=1.40
